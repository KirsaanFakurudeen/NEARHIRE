# Copy this file and fill in your own values.
# These files are NOT committed to the repo — generate them via FlutterFire CLI.

# Firebase Project ID
FIREBASE_PROJECT_ID=your-firebase-project-id

# Google Maps API Key (add to app_constants.dart and AndroidManifest.xml)
GOOGLE_MAPS_API_KEY=your-google-maps-api-key
